# Secrets and local config
.env
*.env
!*.env.example

# .NET build artifacts
bin/
obj/
*.user

# .NET SDK host cache (e.g. runfile-discovery)
dotnet/        

# Docker volumes / runtime data (keep the data file in repo via .gitkeep)
data/*.json

# RAG observability reports (generated, never committed)
reports/

# IDE
.vs/
.idea/
*.swp
