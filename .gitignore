# Secrets and local config
.env
*.env
!*.env.example

# .NET build artifacts
bin/
obj/
*.user
dotnet/        # .NET SDK host cache (e.g. runfile-discovery)

# Docker volumes / runtime data (keep the data file in repo via .gitkeep)
data/*.json

# RAG observability reports (generated, never committed)
reports/

# Created purchase requisitions (generated by the chat create_requisition tool)
requisitions/

# IDE
.vs/
.idea/
*.swp
